apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: liberty-app
  namespace: openshift-gitops
  labels:
    group: applications
  annotations:
    argocd.argoproj.io/sync-wave: "50"
spec:
  destination:
    namespace: liberty-sampleapp
    server: https://kubernetes.default.svc
  project: default
  source:
    path: ./components/liberty/base
  syncPolicy:
    automated:
      prune: true
      selfHeal: true