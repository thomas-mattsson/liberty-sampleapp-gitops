apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: liberty-operator
  namespace: openshift-gitops
  labels:
    group: applications
  annotations:
    argocd.argoproj.io/sync-wave: "50"
spec:
  destination:
    namespace: open-liberty
    server: https://kubernetes.default.svc
  project: default
  source:
    path: ./components/liberty/operator
  syncPolicy:
    automated:
      prune: true
      selfHeal: true